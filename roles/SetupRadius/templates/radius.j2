$npsClients = Get-NpsRadiusClient
$changed = $false

{% for host in groups['Network']%}
if($npsClients -ne {{ inventory_hostname }})
{
    New-NpsRadiusClient -address "{{ hostvars[host].ansible_host }}" -name "{{ inventory_hostname }}" -sharedsecret "{{ Radius_secret }}"
    $changed = $true
}