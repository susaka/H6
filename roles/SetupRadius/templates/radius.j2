$npsClients = Get-NpsRadiusClient
$changed = $false

{% for host.key in groups%}
if($npsClients -ne {{ value }})
{
    New-NpsRadiusClient -address "{{ host.key.ansible_host }}" -name "{{ host.key }}" -sharedsecret "BmEYRA3Y"
    $changed = $true
}

{% endfor %}