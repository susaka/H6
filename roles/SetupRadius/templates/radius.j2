$npsClients = Get-NpsRadiusClient
$changed = $false

{% for host in groups%}
if($npsClients -ne {{ host }})
{
    New-NpsRadiusClient -address "{{ host.ansible_host }}" -name "{{ host }}" -sharedsecret "BmEYRA3Y"
    $changed = $true
}

{% endfor %}