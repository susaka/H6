$npsClients = Get-NpsRadiusClient
$changed = $false

{% for key,value in groups%}
if($npsClients -ne {{ host }})
{
    New-NpsRadiusClient -address "{{ value.ansible_host }}" -name "{{ key }}" -sharedsecret "BmEYRA3Y"
    $changed = $true
}

{% endif %}
{% endfor %}