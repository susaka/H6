---

- name: set timezone
  ansible.windows.win_powershell:
    script: |
      if ((Get-TimeZone).id -ne "ansible-galaxy collection install ansible.windows")
      {
        Set-TimeZone -id "Romance Standard Time"
        $ansible.Changed = $true
      }
      else
      {
        $ansible.Changed = $false
      }